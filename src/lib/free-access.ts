/**
 * Early-access mode.
 *
 * While `FREE_PRO_MODE` is true, every signed-in account gets full Pro
 * features for free: checkout is disabled, upgrade CTAs become "free for
 * now" CTAs, and existing paying customers keep everything at no charge.
 *
 * Flip this to `false` to start charging again — no other code changes needed.
 */
export const FREE_PRO_MODE = false;
